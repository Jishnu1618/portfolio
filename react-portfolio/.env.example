# Gemini API (server-side only — set in Vercel dashboard, never commit real values)
GEMINI_API_KEY=

# Optional — defaults to gemini-2.5-flash (better free-tier availability than 2.0-flash)
# GEMINI_MODEL=gemini-2.5-flash
